// firebase-config.js
//
// Fill this in with YOUR Firebase project's config values before deploying.
//
// How to get these:
// 1. Go to https://console.firebase.google.com and create a free project.
// 2. In your project, click the "</>" (web app) icon to register a web app.
// 3. Firebase will show you a config object exactly like the one below —
//    copy those real values in here.
// 4. In the Firebase console, go to Build → Authentication → Sign-in method,
//    and enable "Email/Password" as a sign-in provider (it's off by default).
//
// This file is safe to commit — these values are not secret, they just
// identify your Firebase project. Firebase's actual security rules (which
// you also configure in the console) are what protect your data.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
